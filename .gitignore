# Rust build artifacts
target/
**/target/
Cargo.lock.bak

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local overrides
**/*.local.toml
.env

# SQLite (apeireth-memory.db)
*.db
*.db-shm
*.db-wal

# Mac metadata (._*)
._*

# Python egg-info (apeireth-sdk/src-py) — src-py 整目录已于 R25 删 (纯 Rust 重写)
*.egg-info/

# AI agent 临时产物 (post-1.0.0 CI 审计 + fix patch 留痕, 应不入 commit)
# 模式: subagent 跑 clippy / fmt / cargo-audit 的输出留痕 + 临时 diff 文件
# 用法: 看但不 commit, 看完 rm
all_rs_files.txt
clippy*.log
fmt_*.log
fmt_per_crate*.log
fmt_per_crate*.txt
fix_*.diff


# R25 防御: apeireth-sdk 虚拟环境 (Cargo.toml 已无 pyo3, 别再创建 .venv)
crates/apeireth-sdk/.venv/

# 一次性 trash (V1271 smoke + 类似)
_v1271_smoke_trash_*/
_*/.trash/

# ad-hoc 检查脚本 (Hermes 16:40 战报后已用完, 留本地)
check_*.ps1
count_*.ps1
debug_test.ps1
head_unwraps.ps1
show_head_line.ps1
test_regex.ps1

# Worktree 临时工具 (引用 UUID path, 不能跨机器跑)
scripts/_*.py
scripts/_*.sh

# Leaked / 反编译源码 (license 风险, 永远不 commit)
research/source/
claude-code-leaked/
vended/
# 商业游戏参考截图 (明日方舟: 终末地 reference — license 风险, 不 commit)
frontend/design-preview/reference/

# 临时 typo 文件
=
=*

# Release signing private keys must never enter the repository
**/cosign.key
**/cosign.key.*

# 台账 #28 续: Companion provider credential files (本地真实 API key, 严禁入库)
# companion_serve 启动读取 apikey-ultra.txt (用户本地管理的 secret).
# 规则精确: 仅忽略 apikey-ultra.txt + apikey-*.txt (连字符变体),
# 不误伤将来可能的 apikey.example.txt (点号分隔规范示例).
apikey-ultra.txt
apikey-*.txt

# Lock the cosign-keygen scratch directory so private keys never enter the repo
reports/.tmp-cosign-keygen/

# 台账 #28 加固 (A4 SEC2): 密钥/证书/密钥库类文件通配忽略
# (此前仅针对性忽略 **/cosign.key, 实测 secret.pem 不被忽略)
*.pem
*.key
*.p12
*.pfx
id_rsa*

# 台账 #28: 调研记忆目录不进仓库
_research_mem/


# R23 清理: 84+ tmp / audit / check / commit-msg / build / test 一次性文件 (仓库根 + reports/ + scripts/)
# 历史: codex/hermes R17-R23 期间累积, 全部为 ad-hoc debug, 不再需要.
# R23+ 起: 每次需要即兴写 /tmp/<name>.ps1 或 New-TemporaryFile, 避免污染仓库根.
.tmp-*.txt
.tmp-*.sh
.tmp-*.ps1
.tmp-*.py
.tmp-*.md
.tmp-*.bak
audit-*.txt
check-*.txt
commit-msg-*.txt
test-*.txt
build-*.txt
.commit-msg-*.txt
.commit-r20-*.txt
.commit-raw*.txt

# R23 一次性 wrk trees (其他分支 worktree 留下, 主仓跨 worktree 0 触碰)
.spectrai-worktrees/

# R23 一次性 backup (升级前/后 snapshot, 留本地)
.tmp-cargo-lock-*.bak
.tmp-cargo-toml-*.bak
.tmp-apeireth-metrics-*.bak
.tmp-extensions-Cargo.lock-*.bak
.tmp-axum-tokio-tungstenite-tree.txt
.tmp-bus-tokio-tungstenite-tree.txt
.tmp-cargo-audit-*.txt
.tmp-cargo-deny-*.txt
.tmp-cargo-metadata-check.txt
.tmp-r21-fix-*.py
.tmp-tokio-tungstenite-*.txt
.tmp-check-*.lock
.tmp-head-cargo.lock

# R23 清理 — Tauri build artifacts
dist/
DEPENDENCY-trees/

# 一次性 backup (.v2-base / .pre-idx.bak) 备份文件不 commit
*.v2-base
*.pre-*.bak

# R23 P3 audit 后续 final 报告（一次性, 留 gitignore 保护避免再变异）
reports/cargo-audit-r23-final-2026-08-06.txt


# R119 Mavis (2026-08-10): _workspace/ 目录约定
# 施工人临时工作副本专用目录: _workspace/README.md + .gitkeep 跟踪, 其他全部忽略
# 用途: 临时脚本 / 临时 log / 临时 backup / worktree 工具
# 规则: 用完即删, 不参与 build, 父目录在 Apeireth-rust/_workspace/
_workspace/_archive/
_workspace/*
!_workspace/README.md
!_workspace/.gitkeep


# R119-5 Mavis (2026-08-10): 根目录 throwaway 临时脚本 (R19-R26 era 留)
# 历史: codex/hermes/codex-r19-r26 累积, 全是 ad-hoc debug, 永不 commit
# R119+ 约束: 一次性脚本请用 /tmp/<name>.ps1 或 New-TemporaryFile, 不要留在根目录
# 临时 Python 脚本 (root only)
/append_*.py
/dump*.py
/debug_*.py
/fix_*.py
/patch_*.py
/loosen_*.py
/trace_*.py
/test_*.py
# 临时 web 模板
/about.hbs
/about.toml
# 临时 web 错误/输出
/apeireth-web.err
/apeireth-web.out
# 临时分析报告
/audit-report.json
/DEPENDENCY
# 0 字节 typo
/=
# R26 era .openclaw build log (root only)
/.openclaw-*.log
# 修真 backup 目录 (R119-1 _v1306_backup + R119-5 .apeireth-a16-build 同性质)
/.apeireth-a16-build/
# Tauri 端 (R19+ 砍前端, 主人 8/4 19:53 决策不归本仓范围)
/src-tauri/

# R125 17:23 Mavis (per decision-33): Python audit + commit msg 草稿 ignore
out/
Apeireth-rust/apeireth/out/
.git_commit_msg.txt

# R126 P2-2 Mavis (2026-08-10): 8 硬墙 验证日志 (per 决策 #33 + 决策 #51 §1.3 P2-2)
# crates/apeireth-integration-e2e/ 6 重守门 v6 (B4) verify 一次性 .log 留本地
# 实际 5 文件: .test.log (test gatekeeper) + .check.log (compile gatekeeper)
#             + .demo.log (behavior gatekeeper) + .all-test.log (full test)
#             + .final-test.log (final verify)
# 6 重守门 v6 验证产物, 0 必进 git
crates/apeireth-integration-e2e/.*.log

# R126 P2-2 Mavis (2026-08-10): R125+ sub-agent scratch (8 硬墙 升级 spec/stub 草稿)
# 8 硬墙 升级期间 (B7 9 organ 内部借 + A3 13 键 PHL-07 等) sub-agent 写的 spec/stub 草稿
# 实际 3 文件: .r125-12-REFACTOR-PLAN.md + .r125-12-13-keys-stub.rs + .r125-12-PHL-07-SPEC.md
# 习惯: .r<round>-<task>-*.<ext> = sub-agent scratch, 0 必进 git (整合 #4 commit 已含 3 实际文件)
.r[0-9][0-9][0-9]-*-*
*.py

# TP20-S5 (2026-08-18 主人拍板): 发布期供应链验证工具链
# 边界: tools/ 是 cargo install --root 落地点 (per crate, 二进制 ~100MB+),
#       sbom/ 是 cyclonedx 收口的 per-crate SBOM (~5MB * 84 = 400MB+),
#       supply-chain/ 是 cargo-vet 初始化产物 (782 exemptions + Mozilla 缓存),
#       3 个目录都不进 git — 重现: `cargo vet init` + `make tools-install`
# 注: 这些目录必须在 init 之前先 .gitkeep, 否则首次 checkout 后丢失规则
tools/
sbom/
supply-chain/

# TP20-S5 SBOM 主输出文件 (cyclonedx-sbom.json) 走 CI artifact, 不进 git
cyclonedx-sbom.json

# TP20-S5: cargo-vet init 时输出, 复用策略同 supply-chain/
imports.lock

# TP20-S5: cargo-audit stderr (audit-report.json 走 CI artifact)
audit-audit.stderr.txt
sbom-cyclonedx.stderr.txt

# R215 BORROW (2026-08-21, from Jimmyxiao2009/apeireth-rust fork 4b230e3c):
# Companion provider credential files (本地真实 API key, 严禁入库)
# companion_serve 启动读取 apikey-ultra.txt (用户本地管理的 secret).
# 规则精确: 仅忽略 apikey-ultra.txt + apikey-*.txt (连字符变体),
# 不误伤将来可能的 apikey.example.txt (点号分隔规范示例).
apikey-ultra.txt
apikey-*.txt
# Generic git credential store (e.g. .git-credentials / Users*.git-credentials).
*.git-credentials
Users*.git-credentials
# R215 续: 报告文件名含 "real-key" / "real-llm" / "minimax-hello" 的, 默认 ignore (防御性):
# 历史有 3 个 reports/ 文件写入了完整 MiniMax API key, 已 redact 留形态占位.
# 后续同命名规范报告不再入库 (避免再泄露). git filter-repo 留后续批.
reports/*real-key*
reports/*real-llm*
reports/*minimax-hello*

# TP13 #31 (backlog #32): crates/apeireth-memory.db{,-shm,-wal} WAL 泄漏 (R15 期间 486KB 进源码树)
# 未跟踪但防御性 ignore, 防止日后 cargo run/init 误入
crates/*.db
crates/*.db-shm
crates/*.db-wal

# TP13 #42 (backlog #42): .scratch_n4/ 验证 harness 残留 (Cargo.lock + target 巨大)
# .scratch_n4/n17-tp2-assembly-summary.md 仍 tracked 作为产物, 其他全 ignore
.scratch_n4/Cargo.lock
.scratch_n4/Cargo.toml
.scratch_n4/src/
.scratch_n4/target/

# TP13 #44 (backlog #28 之外): target/ dist/ build/ coverage/ (重复确认)
# 顶部已有 target/ + **/target/, 此处加 dist/ build/ coverage/ 跨平台
dist/
build/
coverage/
**/dist/
**/build/
**/coverage/

# Frontend (companion-desktop)
**/node_modules/
frontend/companion-desktop/dist/
